# Materials Design frontend – copy to .env or .env.development and adjust
# API base URL for materials (no trailing slash). Endpoints: /summary, /by-id/{id}, /by-material-id/{id}
REACT_APP_BASE_URL=http://localhost:8000/api/materials
# Optional: formula autocomplete endpoint (leave empty if not used)
REACT_APP_AUTOCOMPLETE_URL=
REACT_APP_API_KEY=
